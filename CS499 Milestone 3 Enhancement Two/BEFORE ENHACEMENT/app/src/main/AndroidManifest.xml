<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.telephony" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RileyMest">

        <activity android:name=".ScreenSMS" android:exported="true"/>

        <activity
            android:name=".ScreenName"
            android:theme="@style/Theme.RileyMest.NoActionBar"
            android:exported="true" />

        <activity android:name=".ScreenAddItem"
            android:theme="@style/Theme.TransparentActivity"
            android:exported="true" />

        <activity android:name=".ScreenLogin" android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>